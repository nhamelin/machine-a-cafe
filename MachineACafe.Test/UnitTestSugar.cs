using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace MachineACafe.Test
{
    [TestClass]
    public class SucreTest
    {
        [TestMethod]
        public void Sucre()
        {

        }
    }
}

/*
 * ÉTANT DONNÉ que le client a inséré 40 cts
 * ET qu’il appuie sur « ajouter du sucre » { n } fois
 * QUAND il appuie sur « expresso »
 * ALORS la machine sert un expresso sucré { n } fois dans un gobelet jetable
 * CASE { n } = 0, 1, 3, 5
*/